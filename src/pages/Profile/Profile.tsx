import React, {useState} from "react";
import { useNavigate } from "react-router-dom";

import "./Profile.css";

function Profile() {
    return (
        <main>
            <button onClick={Profile}>Click Event</button>
        </main>
    )
}

export default Profile;